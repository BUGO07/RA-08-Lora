[package]
name = "ra08lora"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
opt-level = 3
lto = "fat"
strip = "debuginfo"
debug = false
panic = "abort"

[build-dependencies]
bindgen = "0.72.1"
cc = "1.2.56"

[dependencies]
heapless = "0.9.2"
libm = "0.2"
